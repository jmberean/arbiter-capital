You are an Expert Lead Systems Architect and Principal Web3/AI Engineer.

Your task is to conduct an EXTENSIVE, deep-dive code review of the entire `arbiter-capital` codebase and RESOLVE all issues found, strictly adhering to the architecture, constraints, and requirements defined in `docs/SYSTEM_DESIGN.md` and `docs/TECHNICAL_ROADMAP.md` (v5.0 "Winning Edition").

### Your Mission:
1. **Extensive Code Review:**
   - Compare the current implementation in the repository against the seven invariants in the System Design (Cognitive isolation, Cryptographic accountability, Deterministic last-mile, Reproducible AI memory, Adversarial resilience, Public verifiability, Decentralized transport).
   - Audit all five agent processes (`quant_process.py`, `patriarch_process.py`, `execution_process.py`, KeeperHub MCP, and `byzantine_watchdog.py`) for correct integration, isolation, and compliance.
   - Ensure the cryptographic consensus (EIP-712 `ConsensusBundle`), threshold ECDSA (2-of-2), and dedupe ledger are correctly implemented and secure.
   - Verify that the KeeperHub Sim Oracle is correctly invoked by the Patriarch, and its signature is verified.
   - Audit the 0G canonical AI memory substrate (`llm_context_writer.py`, `replay_decision.py`, `memory_manager.py`) to ensure every LLM call is hash-chained and deterministic.
   - Review the Uniswap v4 execution path (`ArbiterThrottleHook`, Universal Router, Permit2) for exact alignment with the specs.
   - Check compliance with the ETHGlobal Bounty Hard Requirements listed in the System Design (e.g., Gensyn AXL live mesh, KeeperHub LangChain bridge).

2. **Resolve All Issues Found:**
   - For every discrepancy, bug, or missing feature you find between the codebase and the `SYSTEM_DESIGN.md` / `TECHNICAL_ROADMAP.md`, you MUST write the correct code to resolve it.
   - Do not just leave comments or suggestions. Provide the exact, production-ready code modifications necessary to fix the issues and implement the missing pieces of the v5.0 Elite Features.
   - Ensure all fixes maintain the "math-first cognition" rule, strict decoupling of agents, and decimals discipline (base-units strings).

### Context Files:
Please read the following files carefully before beginning your review and implementation:
- `docs/SYSTEM_DESIGN.md`
- `docs/TECHNICAL_ROADMAP.md`

Output your review findings followed by all the corresponding code fixes immediately. Leave no issue unresolved.